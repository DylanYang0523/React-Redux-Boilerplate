// Test your front-end against a real API from https://reqres.in/api
const baseUrl = 'https://reqres.in/api';
const config = {
    login: `${baseUrl}/login`,
    users: `${baseUrl}/users`,
};

export default config;
